# Contextual Bug Analysis - Visual Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     NEW BUG RECEIVED FROM JIRA                      │
│                    "Login fails with NPE"                           │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│               STEP 1: GENERATE EMBEDDING (384-dim)                  │
│   Embedding Service: "Login fails with NPE" → [0.21, -0.45, ...]   │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│           STEP 2: SEARCH OPENSEARCH (k-NN Semantic Search)          │
│             Query: embedding vector (384 dimensions)                │
│             Threshold: similarity ≥ 70%                             │
│             Limit: Top 3 results                                    │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                ┌────────────────┴────────────────┐
                │ Found similar bugs?             │
                └────┬────────────────────────┬───┘
                     │ NO                     │ YES
                     ▼                        ▼
        ┌────────────────────┐    ┌──────────────────────────┐
        │ historical_context │    │ Similar Bug #1 (85.3%)   │
        │ = None             │    │ Bug: ABC-123             │
        │                    │    │ Issue: NPE in auth       │
        │ Continue without   │    │ Fix: Added null check    │
        │ context            │    │                          │
        └─────────┬──────────┘    │ Similar Bug #2 (78.1%)   │
                  │               │ Bug: ABC-087             │
                  │               │ Issue: User validation   │
                  │               │ Fix: Null-safe access    │
                  │               └─────────┬────────────────┘
                  │                         │
                  │                         ▼
                  │              ┌──────────────────────────┐
                  │              │ STEP 3: FORMAT CONTEXT   │
                  │              │ _format_historical_      │
                  │              │    context()             │
                  │              │                          │
                  │              │ Creates formatted string │
                  │              │ with bug details +       │
                  │              │ findings from history    │
                  │              └─────────┬────────────────┘
                  │                        │
                  └────────────┬───────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│         STEP 4: SEND TO CLAUDE FOR ANALYSIS                         │
│                                                                     │
│ Prompt includes:                                                    │
│ ┌─────────────────────────────────────────────────────────────┐   │
│ │ === HISTORICAL CONTEXT ===                                  │   │
│ │ Bug #1 (85.3%): ABC-123 - NPE in auth                       │   │
│ │   File: UserService.java                                    │   │
│ │   Fix: if (user != null && user.getEmail() != null) {...}  │   │
│ │                                                              │   │
│ │ Bug #2 (78.1%): ABC-087 - User validation                   │   │
│ │   File: AuthController.java                                 │   │
│ │   Fix: Optional.ofNullable(user).map(...)                   │   │
│ │ === END CONTEXT ===                                          │   │
│ │                                                              │   │
│ │ Current Bug: "Login fails with NPE"                         │   │
│ │ [Code files to analyze...]                                  │   │
│ └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│ Claude analyzes with historical context as reference                │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│              STEP 5: RECEIVE CLAUDE'S ANALYSIS                      │
│                                                                     │
│ ✓ Identified file: src/auth/LoginService.java                      │
│ ✓ Lines: 45-48                                                      │
│ ✓ Issue: Missing null check (consistent with ABC-123)              │
│ ✓ Resolution: Add null validation (pattern from history)           │
│ ✓ Code Fix: if (user != null && user.getEmail() != null) {         │
│              // ... access properties                               │
│            }                                                        │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│              STEP 6: GENERATE REPORT                                │
│   - JSON format with all findings                                  │
│   - Markdown format for readability                                │
│   - Save to reports/ directory                                     │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│      STEP 7: LOG TO HISTORY (for future contextual analysis)        │
│                                                                     │
│ Check for duplicates first:                                         │
│ - If similarity > 90%: Skip logging (duplicate)                     │
│ - Otherwise: Log to OpenSearch                                      │
│                                                                     │
│ OpenSearch Index: bug_analysis_logs                                 │
│ ┌─────────────────────────────────────────────────────────────┐   │
│ │ Document: {                                                  │   │
│ │   bug_key: "ABC-999",                                        │   │
│ │   bug_summary: "Login fails with NPE",                       │   │
│ │   embedding: [0.21, -0.45, 0.67, ...],  # 384 dimensions    │   │
│ │   findings: [{                                               │   │
│ │     file: "LoginService.java",                               │   │
│ │     issue: "Missing null check",                             │   │
│ │     resolution: "Add null validation"                        │   │
│ │   }],                                                         │   │
│ │   timestamp: "2025-12-16T10:30:00"                           │   │
│ │ }                                                             │   │
│ └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│ This analysis is now available for future bugs!                     │
│ (unless it was skipped as a duplicate)                              │
└─────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
                            KEY BENEFITS
═══════════════════════════════════════════════════════════════════════

✓ CONSISTENCY: Solutions align with previous fixes
   Example: All NPE issues get same null-check pattern

✓ LEARNING: System improves with each analysis
   More history = Better context = Smarter fixes

✓ EFFICIENCY: Claude has reference points
   No need to reinvent solutions for similar bugs

✓ PATTERN RECOGNITION: Identifies recurring issues
   Highlights architectural problems needing refactoring

═══════════════════════════════════════════════════════════════════════

SIMILARITY SCORING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 0%            50%           70%           80%           90%         100%
 │              │             │             │             │             │
 └──────────────┴─────────────┼─────────────┼─────────────┼─────────────┘
                              │             │             │
                       Threshold     Good Match    Duplicate Threshold
                    (Minimum 70%)   (Reference)   (Not logged >90%)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DUPLICATE PREVENTION:
- Bugs with >90% similarity are NOT re-logged
- Prevents redundant entries in log history
- Analysis still proceeds with historical context
- Only the logging step is skipped

CONFIGURATION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Setting                  Default         Purpose
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ENABLE_LOG_HISTORY       true            Enable/disable feature
  min_score                0.7             Similarity threshold
  limit                    3               Max similar bugs to return
  embedding_dims           384             Vector dimensions
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PERFORMANCE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Operation                    Time         Impact
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Generate embedding           ~100ms       One-time per bug
  OpenSearch k-NN query        ~50ms        Fast vector search
  Format context               ~10ms        String manipulation
  ──────────────────────────────────────────────────────────────────
  Total overhead               ~160ms       Minimal impact
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Example: Real-World Impact

### Without Contextual Analysis
```
Bug 1: NPE in auth → Fix: if (user != null) check
Bug 2: NPE in profile → Fix: try-catch block
Bug 3: NPE in settings → Fix: Optional.ofNullable()

Result: Inconsistent approaches, harder to maintain
```

### With Contextual Analysis
```
Bug 1: NPE in auth → Fix: if (user != null) check
Bug 2: NPE in profile → Sees Bug 1 context → Uses same pattern
Bug 3: NPE in settings → Sees Bugs 1 & 2 → Consistent approach

Result: Uniform null-safety pattern across codebase
```

## When Historical Context is Provided

```
Console Output:
══════════════════════════════════════════════════════════════
Bug 3/10: ABC-999
══════════════════════════════════════════════════════════════
Summary: Login fails with NullPointerException
Status: Open
Priority: High

  🔍 Searching log history for similar bugs...
  ✓ Found 2 similar bug(s) in history

Analyzing bug: ABC-999
══════════════════════════════════════════════════════════════

Analyzing 25 .java files...
  Sending 3 files to Claude for analysis...
  ✓ Received analysis from Claude

✓ Analysis complete: 3 findings
  (Claude used historical context from ABC-123 and ABC-087)
══════════════════════════════════════════════════════════════
```

This shows the system working with historical awareness!
